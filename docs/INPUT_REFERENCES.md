# 输入资料说明

该工程根据以下项目输入实现：

- 后端模块设计 B01–B12。
- 后端开发规则、功能清单与 share/outbox 公共模块约束。
- LCT 最新高保真 UI 导入包及历次页面修订要求。
- 前一版《LCT Vite + React 前端工程架构构建方案》。

压缩包不重复包含体积较大的原始后端文档和设计图。联调时应把正式 OpenAPI 作为 HTTP 契约唯一来源；本项目中的 endpoint、Mock payload 和类型是可运行的起步示例，不应替代后端最终契约。
