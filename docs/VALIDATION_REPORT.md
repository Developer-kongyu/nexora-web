# 交付验证报告

验证日期：2026-07-28

## 1. 已完成检查

| 检查项                  | 结果         | 说明                                                                                                                                                               |
| ----------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TypeScript 源码静态检查 | 阶段扫描通过 | 使用环境内全局 TypeScript 扫描 `src + tests` 共 177 个 TS/TSX 文件；其中 175 个为非声明文件。独立语法和本地导入解析通过；完整项目语义 typecheck 仍需依赖安装后执行 |
| 本地模块解析            | 通过         | 177 个 TS/TSX 文件，无未解析的相对路径或 `@/` 别名导入                                                                                                             |
| 分层依赖                | 通过         | 无 `shared/domains/widgets/pages/app` 逆向或越层依赖                                                                                                               |
| 模块循环依赖            | 通过         | 源码导入图未发现循环                                                                                                                                               |
| 裸网络请求              | 通过         | 唯一 `fetch` 位于 `src/shared/api/client.ts`；页面和业务组件无裸请求                                                                                               |
| 路由覆盖                | 通过         | `ROUTE_MAP.md` 中 33 个页面组件均在 Router 中配置；Router 额外包含统一 404 页面                                                                                    |
| 高保真素材              | 通过         | `design-reference/png_preview` 包含 31 张设计图，文件名完整                                                                                                        |
| CSS 语法解析            | 通过         | 54 份 CSS 经独立解析，0 error                                                                                                                                      |
| CSS Modules 引用        | 通过         | 未发现源码引用不存在的模块类名                                                                                                                                     |
| CSS 自定义变量          | 通过         | 未发现 `var(--token)` 引用未声明变量                                                                                                                               |
| 残留占位标记            | 通过         | 源码无 TODO、FIXME、“起步骨架”“占位页面”“仅供演示”等交付残留                                                                                                       |
| 交互按钮审计            | 通过         | 未发现无行为的 Button、IconButton 或原生 button                                                                                                                    |
| 基础可访问性审计        | 通过         | 未发现原生 button 缺少 type、图片缺少 alt、外链缺少安全 rel、可点击非语义节点缺少键盘行为                                                                          |
| 中文文件名              | 通过         | 原压缩包中编码异常的中文文档名已恢复为正常文件名                                                                                                                   |
| 交付目录清理            | 通过         | 不包含 node_modules、dist、coverage、测试报告、临时声明或本机缓存                                                                                                  |

源码统计：`src + tests` 共 177 个 TS/TSX 文件，其中 175 个为非声明文件；54 份 CSS；非声明 TS/TSX 与 CSS 合计约 26,656 行。项目共 319 个文件（包含文档、高保真图、测试和部署配置，不含 Git、依赖目录与构建产物）。

## 2. 未能在当前环境执行的命令

以下命令依赖完整 `node_modules`，本次环境无法从被强制配置的内部 npm 代理完整安装依赖，因此未在此环境中声称通过：

```bash
npm run format:check
npm run typecheck
npm run lint
npm run lint:css
npm run test
npm run build
npm run storybook:build
npm run test:e2e:list
npm run test:e2e
```

多次执行 `npm ci` 时，内部代理返回 HTTP 503；阶段 006 的可诊断失败在 `zustand@5.0.14` 返回 E503，阶段 007 的一次安装在代理无有效输出时超时。阶段 008 使用快速失败参数再次复验，内部 registry 仍对 `zustand` 返回 E503；不完整 `node_modules` 已删除。该限制来自交付运行环境，并不代表上述命令已经通过，也不代表源码包包含构建产物。

## 3. 在正常开发机上的复验方式

```bash
npm ci
npm run check
npm run storybook:build
npm run test:e2e:list
```

首次执行浏览器 E2E 时：

```bash
npx playwright install chromium
npm run test:e2e
```

建议在 CI 中使用与 `package-lock.json` 一致的 npm registry，并保留 `.github/workflows/ci.yml` 中的质量门禁。

## 4. 结论

源码结构、页面路由、领域边界、样式引用、核心交互与可访问性静态检查均已完成并通过。正式构建、Lint 和自动化测试需要在依赖可正常安装的开发机或 CI 中复验；本报告未把无法执行的项目误标为通过。

---

## 5. 2026-07-28 阶段 002 增量验证

本阶段对通知中心、实时通知初始化、关注请求审批、用户公开资料与关系操作进行了合同级整改。

| 检查项                              | 结果               | 说明                                                                                         |
| ----------------------------------- | ------------------ | -------------------------------------------------------------------------------------------- |
| TS/TSX 独立语法解析                 | 通过               | 使用环境内全局 TypeScript 5.8.3 对 134 个非声明 TS/TSX 文件执行 `transpileModule`，0 error   |
| 静态/伪成功交互检查                 | 通过（本阶段范围） | 通知申请、关注、静音、屏蔽均已替换为 API Mutation；失败不会显示成功结果                      |
| 实时 bootstrap 合同                 | 通过               | 已由不存在的 `token/cursor` 修正为 B11 的 `summary/latestSeq`                                |
| Mock 状态闭环                       | 通过（本阶段范围） | 已读、全部已读、审批、关注、静音和屏蔽会改变后续查询结果                                     |
| 完整依赖安装                        | 阻断               | 2026-07-28 再次尝试安装，内部 npm registry 对多个包返回 HTTP 503；临时 `node_modules` 已删除 |
| `npm run typecheck/lint/test/build` | 未执行             | 依赖不完整，未将无法执行的命令标为通过                                                       |

阶段 002 的详细文件级变更见 `docs/CHANGELOG.md`。

---

## 6. 2026-07-28 阶段 003 增量验证

本阶段对帖子详情中的顶层评论读取、评论回复写入、评论删除、评论点赞和删除占位进行了合同级整改。

| 检查项                              | 结果                     | 说明                                                                                   |
| ----------------------------------- | ------------------------ | -------------------------------------------------------------------------------------- |
| TS/TSX 独立语法解析                 | 通过                     | 使用环境内全局 TypeScript 对 135 个非声明 TS/TSX 文件执行 `transpileModule`，0 error   |
| 本地模块解析                        | 通过                     | 扫描 136 个 TS/TSX 文件，相对路径与 `@/` 别名未发现无法解析项                          |
| CSS 语法解析                        | 通过                     | 使用 `tinycss2` 解析 53 份 CSS，0 error                                                |
| Git 空白与冲突标记检查              | 通过                     | `git diff --check` 无错误                                                              |
| B04/B06 评论合同核对                | 通过（当前公开能力范围） | 路由、query 参数、compose body、relation/tombstone DTO 和删除 outcome 已按后端文档修正 |
| Mock 状态闭环                       | 通过（静态检查）         | 创建、回复、删除和根帖评论计数会改变后续 Mock 结果；删除项保留 tombstone               |
| 评论 API 自动化用例                 | 已新增、未执行           | 新增 canonical body、列表 tombstone、创建/回复/删除测试；依赖未安装，未声称运行通过    |
| `npm run typecheck/lint/test/build` | 未执行                   | 内部 npm registry 持续 HTTP 503，完整依赖不可用                                        |

### 已确认的接口限制

B04 公开 `GET /api/posts/:postId/replies` 只接受 `postId / cursor / limit`，内部 B06 虽支持 `parentCommentId` child page，但当前没有 controller 对前端公开该参数。因此本阶段没有保留错误的递归请求，也没有用静态数组伪造楼中楼历史。回复写入正常调用正式接口，读取能力缺口单独记录于 `docs/KNOWN_ISSUES.md`。

---

## 7. 2026-07-28 阶段 004 增量验证

本阶段将社群管理后台从静态管理视图整改为 B08 正式 API、DTO、Query/Mutation 与状态化 Mock 闭环。

| 检查项                              | 结果                       | 说明                                                                                                                 |
| ----------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| TS/TSX 独立语法解析                 | 通过                       | 全局 TypeScript 5.8.3 扫描 147 个非声明文件，0 error                                                                 |
| 本地模块解析                        | 通过                       | 扫描 149 个 TS/TSX 文件，0 个无法解析的相对路径或 `@/` 别名                                                          |
| 未使用导入检查                      | 通过                       | AST 扫描 149 个 TS/TSX 文件，0 个未使用 import binding                                                               |
| 社群合同类型子图                    | 通过                       | 对 `communities/model`、fixtures 与状态化 handlers 执行严格空值/索引检查；B08 moderation metadata 判别联合无类型错误 |
| CSS 语法解析                        | 通过                       | `tinycss2` 解析 53 份 CSS，0 error                                                                                   |
| CSS Modules 引用                    | 通过                       | 扫描 TS/TSX 的 `styles.xxx` 使用，0 个缺失类名                                                                       |
| Git 空白、冲突标记、空文件          | 通过                       | `git diff --check` 无错误；未发现冲突标记或空源码/文档                                                               |
| B08 管理路由核对                    | 通过（本阶段范围）         | 详情、概览、申请审批、成员、置顶、规则、设置和日志路径与 body 已按文档修正                                           |
| Mock 状态闭环                       | 通过（类型与代码路径检查） | 审批、角色、移除、置顶、规则和设置会同步影响后续列表、概览、版本和日志                                               |
| 社群 API 自动化用例                 | 已新增、未执行             | 新增 5 组 API 合同用例；依赖未安装，未声称 Vitest 运行通过                                                           |
| `npm run typecheck/lint/test/build` | 未执行                     | 内部 npm registry 持续 HTTP 503，完整依赖不可用                                                                      |

### 明确的产品能力边界

当前 B08 没有公开社群举报列表/审批 controller，也没有独立自由文本公告资源。前端因此使用正式的帖子置顶 `pinType=ANNOUNCEMENT` 表达公告，并对举报管理显示不可用状态，不再用静态数组或 Toast 伪造处理成功。

---

## 8. 2026-07-28 阶段 005 增量验证

本阶段将社群创建图片从 Base64/演示数据整改为 B05 上传会话、对象直传、确认、处理状态与 B08 正式创建 DTO。

| 检查项               | 结果                 | 说明                                                                       |
| -------------------- | -------------------- | -------------------------------------------------------------------------- |
| 上传会话响应身份校验 | 通过（代码路径检查） | 单文件必须恰好返回一项，且 `clientUploadId / scene / assetKind` 与请求一致 |
| 上传断点绑定         | 通过（代码路径检查） | 检查点绑定 scene、文件名、lastModified、MIME、大小和 client upload ID      |
| FAILED 自动恢复      | 通过（代码路径检查） | 只调用一次正式 retry，第二次 FAILED 明确失败，不会无限重试                 |
| 社群创建 DTO         | 通过（合同核对）     | 只提交 B08 正式创建字段，媒体只使用 READY storage key                      |
| Mock 状态闭环        | 通过（静态检查）     | 上传、确认、retry、READY 与社群媒体场景校验会改变后续结果                  |
| 自动化用例           | 已新增、未执行       | 新增上传、断点、retry 和创建幂等/字段测试；依赖不可用，未声称 Vitest 通过  |

## 9. 2026-07-28 阶段 006 增量验证

本阶段完成 B02 owner-only 资料读取/更新、头像与封面 B05 上传复用，并重新核对编辑资料高保真结构。

| 检查项                              | 结果                 | 说明                                                                                                                                                 |
| ----------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| TS/TSX 独立语法解析                 | 通过                 | 全局 TypeScript 扫描 162 个非声明文件，0 error                                                                                                       |
| 本地模块与 import binding           | 通过                 | 0 个无法解析的相对/别名导入，0 个未使用 import binding                                                                                               |
| B02/B05 类型子图                    | 通过                 | 使用临时 API client/pagination 声明对 users DTO/API 与 media DTO/API/上传编排执行 strict + noUncheckedIndexedAccess；临时文件位于 `/tmp`，未写入源码 |
| CSS 与 CSS Modules                  | 通过                 | 53 个 CSS 文件 0 解析错误；0 个缺失模块类名                                                                                                          |
| 分层依赖                            | 通过                 | 生产层未发现向上层或测试/Mock 层反向依赖；测试、Story 与开发 Mock bootstrap 按边界豁免                                                               |
| 模块循环依赖                        | 通过                 | 本地 TypeScript 导入图 0 个循环                                                                                                                      |
| 网络入口                            | 通过                 | 唯一裸 `fetch` 仍位于 `src/shared/api/client.ts`                                                                                                     |
| 源码完整性                          | 通过                 | 0 冲突标记、0 空源码/文档、0 TODO/FIXME/演示残留                                                                                                     |
| 交互与基础可访问性                  | 通过                 | 0 无行为 Button、0 原生 button 缺 type、0 img 缺 alt、0不安全新窗口链接、0 非语义点击节点缺键盘行为                                                  |
| CSS 自定义变量                      | 通过                 | 0 个 `var(--token)` 引用未声明变量                                                                                                                   |
| B02 资料合同                        | 通过（合同核对）     | GET owner view、七字段 PATCH、URL/date-only 和 storage key 语义与 B02 文档一致                                                                       |
| B05 头像/封面链                     | 通过（代码路径检查） | 会话、对象上传、confirm、retry、READY、断点恢复与响应身份防御均已覆盖                                                                                |
| 自动化用例                          | 已新增、未执行       | 新增 media API、上传编排、七牛适配、users API 和资料模型测试；依赖不可用                                                                             |
| `git diff --check`                  | 通过                 | 无尾随空格或补丁空白错误                                                                                                                             |
| `npm run typecheck/lint/test/build` | 未执行               | `npm ci` 最终在 `zustand@5.0.14` 遇到内部 registry HTTP 503/E503，未保留 node_modules                                                                |

### 环境失败记录

本阶段执行过：

```bash
npm ci --ignore-scripts --no-audit --no-fund --prefer-offline
```

npm 最终错误为 E503，日志路径：

```text
/home/oai/.npm/_logs/2026-07-27T23_57_11_439Z-debug-0.log
```

该路径仅用于说明当前运行环境的验证阻断，不进入源码包。

## 10. 2026-07-28 阶段 007 增量验证

本阶段将粉丝/关注页面与屏蔽/静音设置从旧展示模型和硬编码数据整改为 B02 正式 cursor、关系快照、owner 管理列表与动作结果合同。

| 检查项                     | 结果                   | 说明                                                                                                                                               |
| -------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| TS/TSX 独立语法与本地导入  | 通过                   | 扫描 `src + tests` 共 170 个 TS/TSX 文件，0 个语法错误、0 个无法解析的相对路径或 `@/` 导入                                                         |
| 生产源码 import 与循环依赖 | 通过                   | 扫描 `src` 共 169 个 TS/TSX 文件，0 个未使用 import binding、0 个本地循环依赖                                                                      |
| B02 关系类型子图           | 通过                   | 对 users DTO/API、关系模型、页面、测试和 Mock 执行临时外部依赖隔离下的 `strict + noUncheckedIndexedAccess` 检查；临时声明只位于 `/tmp`             |
| 纯关系模型运行检查         | 通过                   | 将关系动作、关系列表筛选/排序和安全管理模型编译为临时 CommonJS 后执行 13 项 Node 断言，全部通过                                                    |
| CSS 与 CSS Modules         | 通过                   | 53 个 CSS 文件 0 解析错误、0 缺失自定义属性；CSS Modules 引用 0 个缺失类名                                                                         |
| 分层与网络边界             | 通过                   | 0 个层级逆向依赖；唯一裸 `fetch` 仍位于 `src/shared/api/client.ts`                                                                                 |
| 源码完整性                 | 通过                   | 0 冲突标记、0 空源码、0 TODO/FIXME/HACK 或演示占位残留                                                                                             |
| B02 cursor 合同            | 通过（代码与文档核对） | followers/following、incoming request、mutes、blocks 使用 `{ list, nextCursor }`；本阶段 B02 surface 未残留 `hasMore` 或 `auth:false` 强制匿名请求 |
| 关系动作合同               | 通过（代码与文档核对） | 关注、取关、取消请求、静音、取消静音、屏蔽和解除屏蔽使用精确 `actionResult`；Handle 未命中与 post-commit degraded success 明确区分                 |
| 管理列表占位语义           | 通过（代码路径检查）   | `PLACEHOLDER` 不伪造资料；blocked item 只有 Handle 可用且 `canUnblock=true` 时可解除                                                               |
| API/模型自动化用例         | 已新增、未执行         | users API 与两个纯模型测试文件已写入；依赖不可用，未声称 Vitest 已执行                                                                             |
| `git diff --check`         | 通过                   | 无尾随空格、空白错误或冲突补丁                                                                                                                     |
| 完整 `npm run check/build` | 未执行                 | 本阶段 `npm ci` 在 180 秒内无有效 registry 响应并超时；残留进程和部分依赖已清理                                                                    |

### 阶段 007 验证产物

阶段外部报告保存了静态扫描、源码质量、工程审计、目标类型检查和纯模型运行结果。正式阶段报告与 SHA-256 文件位于交付备份目录旁，不写入应用运行包。

## 11. 2026-07-28 阶段 008 增量验证

本阶段以正式阶段 007 用户关系提交为基线，集成 B07 收藏夹、内容中心、浏览历史以及 B04/B07 草稿链路整改；两条来源分支、Git bundle 和中间检查点均保持可恢复。

| 检查项                              | 结果                       | 说明                                                                                                     |
| ----------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------- |
| TS/TSX 独立语法解析                 | 通过                       | 全局 TypeScript 扫描 `src + tests` 共 177 个文件，0 个 parse error                                       |
| 本地模块解析                        | 通过                       | 相对路径与 `@/` 别名 0 个无法解析项                                                                      |
| 未使用导入与循环依赖                | 通过                       | 源码 AST 扫描 176 个 TS/TSX 文件，0 个未使用 import binding、0 个本地循环                                |
| CSS 与 CSS Modules                  | 通过                       | 54 个 CSS 文件 0 个解析错误；0 个 `styles.xxx` 缺失类名；0 个未声明 CSS 变量引用                         |
| 分层、网络与完整性                  | 通过                       | 0 个层级逆向依赖；唯一裸 `fetch` 位于统一 API client；0 冲突标记、空源码/文档或残留占位标记              |
| 基础可访问性                        | 通过                       | 0 原生按钮缺 type、图片缺 alt、不安全新窗口链接、无标签 IconButton、嵌套交互或缺键盘行为的非语义点击节点 |
| B02 与 B04/B07 目标类型子图         | 通过                       | 两组临时外部依赖隔离下的 `strict + noUncheckedIndexedAccess` 检查通过；临时声明只位于 `/tmp`，不进入源码 |
| 纯模型运行断言                      | 通过                       | 实际编译并执行用户关系 13 项、批处理与帖子卡适配 10 项，共 23 项断言                                     |
| 集成后语义修复                      | 通过                       | 修复收藏 Mock 判别联合在移动/回迁时的非空卡片收窄，以及社群创建 `Promise.allSettled` 成功结果收窄        |
| B07 路由与 DTO                      | 通过（合同核对）           | 收藏夹、收藏项、内容中心三类列表、草稿批量删除、历史单删/清空均使用正式路径和 body                       |
| B04 草稿发布/删除                   | 通过（合同核对）           | 发布携带幂等键和 `allowWaitingMediaPublish`；删除读取正式 outcome；没有调用不存在的 B04 批量删除路径     |
| 批量部分失败                        | 通过（代码路径检查）       | 草稿发布、历史删除和收藏批处理保留逐项结果；成功项更新缓存，失败项继续保留和选中                         |
| 已删除内容能力边界                  | 通过                       | 后端未开放恢复/永久清理时只读展示，不显示伪成功操作                                                      |
| Mock 状态闭环                       | 通过（静态与目标类型检查） | 用户关系、收藏唯一关系、移动/移除、草稿发布/删除、浏览历史删除/清空会改变后续查询结果                    |
| 自动化用例                          | 已新增、未执行             | 新增 library、draft、repost、adapter 和 bounded batch 测试；依赖不可用，未声称 Vitest 通过               |
| `git diff --check`                  | 通过                       | 无尾随空格或补丁空白错误                                                                                 |
| `npm run typecheck/lint/test/build` | 未执行                     | 阶段 008 快速失败复验仍在内部 registry 的 `zustand` 请求上返回 HTTP 503；不完整 `node_modules` 已清理    |

### 当前公开能力边界

B07 没有已删除内容恢复、永久清理或浏览历史批量删除 controller。内容中心因此只读展示已删除记录；历史“删除所选”以有界并发组合正式单项删除接口，并明确它不是服务端原子批处理。该限制和后端建议已记录在 `docs/KNOWN_ISSUES.md`。

## 12. 2026-07-28 阶段 009 增量验证

本阶段以阶段 008 集成提交为基线，针对“相同功能、类型定义、枚举、常量、缓存 key 和状态机重复实现”执行全量治理，并增加可持续阻止回归的 AST 门禁。

| 检查项                      | 结果                   | 说明                                                                                                                                                                                                 |
| --------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 复用门禁                    | 通过                   | `npm run reuse:check` 实际扫描 188 个生产文件；生产 AST 审计覆盖 328 个声明、93 个字面量联合、64 个静态常量和 205 个函数，0 重复导出合同、同构声明、重复字面量联合、静态常量、函数克隆或公共能力绕过 |
| 全源码重复与相似度审计      | 通过                   | 扫描 222 个源码/测试文件，覆盖 342 个声明、95 个字面量联合、117 个静态常量和 248 个函数；0 重复导出名、0 同构声明、0 重复字面量联合、0 重复静态常量、0 精确/结构函数克隆，接口与函数相似度候选均为 0 |
| TS/TSX 独立语法解析         | 通过                   | 扫描 `src + tests` 共 221 个非声明文件，0 parse error                                                                                                                                                |
| 本地模块解析                | 通过                   | 相对路径与 `@/` 别名 0 个无法解析项                                                                                                                                                                  |
| 未使用导入与循环依赖        | 通过                   | 生产依赖图覆盖 187 个非声明 TS/TSX 文件，0 未使用 import binding、0 本地循环依赖                                                                                                                     |
| CSS 与 CSS Modules          | 通过                   | 54 个 CSS 文件 0 解析错误；0 个缺失 CSS Module 类名；0 个未声明 CSS 变量引用                                                                                                                         |
| 分层、网络与完整性          | 通过                   | 0 层级逆向依赖；唯一裸 `fetch` 仍在统一 API client；0 冲突标记、空源码文件或 TODO/FIXME/HACK 残留                                                                                                    |
| 共享合同目标类型检查        | 通过                   | cursor、query、缓存变换、共享模型、媒体/社群/library presentation 与纯函数子图在临时外部依赖声明下通过 `strict + noUncheckedIndexedAccess`                                                           |
| Hook/Query 集成目标类型检查 | 通过                   | 选择、乐观布尔切换、复制反馈、草稿选择与领域 Query Key 子图通过严格检查；临时声明未写入源码                                                                                                          |
| 公共纯函数运行断言          | 通过                   | 临时转译后实际执行 41 项断言，覆盖 query/cursor、Infinite Data、Set、文本、日期、URL、选项、草稿标题、图片校验、批量结果和占位文案                                                                   |
| 查询与分页复用              | 通过（代码路径检查）   | 领域 API 不再自行创建 `URLSearchParams`；cursor 参数、next-page 和缓存变换使用共享实现                                                                                                               |
| 类型与枚举 owner            | 通过（AST + 人工复核） | 同语义 DTO/联合/枚举已回收到 shared 或 owner 领域；只保留不同 owner、权限或演进方向的语义类型                                                                                                        |
| 图片上传流程                | 通过（代码路径检查）   | 资料与社群图片共享 B05 文件校验、选择、上传、确认、失败重试和 READY 编排；旧页面级 Hook 已删除                                                                                                       |
| 列表选择与批处理            | 通过（代码路径检查）   | 收藏、内容中心、草稿、历史使用共享键选择；草稿批量结果汇总和 Infinite Query 删除只保留一个实现                                                                                                       |
| Query Key                   | 通过（代码路径检查）   | feed/posts/users/library/communities/notifications/search/settings 使用领域 key factory；Provider 与 Mutation 不再手写根 key                                                                         |
| 低置信语法形状复核          | 通过（人工复核）       | 仅有各表单 `z.infer<typeof schema>` 和两个不同领域 fallback 的抽象语法相似；它们不共享合同或生命周期，保留独立语义以避免过度抽象                                                                     |
| `git diff --check`          | 通过                   | 无尾随空格、冲突补丁或空白错误                                                                                                                                                                       |
| 完整 `npm run check/build`  | 未执行                 | `npm ci --offline` 在内部 registry 的 `zustand@5.0.14` 返回 `ENOTCACHED`；在线安装此前也无可用响应。不完整 `node_modules` 已清理                                                                     |

### 可持续门禁

`package.json` 的 `check` 命令现在先执行 `reuse:check`，再进入格式、类型、Lint、测试和构建。新增 `docs/REUSE_GUIDELINES.md` 明确权威实现位置和“同语义合并、同值不同语义不强并”的评审规则，避免后续为了局部开发重新复制 DTO、枚举或流程。

## 13. 2026-07-28 阶段 011 增量验证

本阶段继续处理阶段 010 工具链预检中暴露的发帖编辑器、草稿恢复和媒体上传合同问题，并在任何大范围修改前保留只读源码与补丁检查点。

| 检查项                   | 结果                   | 说明                                                                                                                                              |
| ------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 复用门禁                 | 通过                   | `npm run reuse:check` 实际扫描 196 个生产文件；0 个重复类型/枚举、静态常量、函数克隆或公共能力绕过                                                |
| TS/TSX 静态预检          | 通过                   | AST 扫描 240 个源码/测试文件；0 语法错误、0 本地导入错误、0 未使用 import、0 React Refresh 混合导出                                               |
| Hooks 规则预检           | 通过                   | 扫描 234 个文件；0 条条件 Hook、嵌套回调 Hook、render setter、render ref 或同步 effect 写状态问题                                                 |
| CSS 与 CSS Modules       | 通过                   | 54 个 CSS、50 个 CSS Module；0 解析错误、0 缺失类名、0 未声明 CSS 变量                                                                            |
| 依赖隔离类型预检         | 通过                   | 源码、测试、构建配置与 E2E 分别在临时外部模块声明下通过 `strict + noUncheckedIndexedAccess`；临时声明不进入仓库，不能替代真实依赖的正式 typecheck |
| Mock 目标类型检查        | 通过                   | 使用精确 MSW 和用户资料 DTO 临时声明检查完整状态化 handlers；0 诊断                                                                               |
| 发帖集成测试目标类型检查 | 通过                   | 对草稿 API、媒体 API、发帖纯合同与新增 Mock 集成测试执行严格检查；0 诊断                                                                          |
| 纯运行断言               | 通过                   | 发帖字段规范化/指纹 7 项，媒体 MIME/大小/状态 12 项，共 19 项 Node 断言                                                                           |
| 草稿版本状态机           | 通过（代码与测试覆盖） | 创建、详情、自动保存、手动保存、409 版本冲突、列表同步、发布后移除均有状态化 Mock 和集成用例                                                      |
| 媒体合同                 | 通过（代码与测试覆盖） | 图片/视频 MIME、大小和数量使用唯一约束模块；帖子视频上传确认保持 `VIDEO`，超限请求返回明确拒绝                                                    |
| 提交指纹                 | 通过（运行断言）       | `bodyTextNormalized` 等输出字段不会污染 `PostComposeInput` 指纹；恢复草稿不会无条件显示未保存                                                     |
| `git diff --check`       | 通过                   | 无尾随空格、补丁空白或冲突标记                                                                                                                    |
| 正式八项工具链           | 被环境阻断             | 2026-07-28 09:00 UTC 复验内部 registry `/-/ping` 仍为 HTTP 503，无法安装真实依赖                                                                  |

### 正式工具执行边界

本阶段没有把 AST、临时声明类型检查或 Node 断言冒充为以下正式命令：

```bash
npm run format:check
npm run typecheck
npm run lint
npm run lint:css
npm run test
npm run build
npm run storybook:build
npm run test:e2e
```

这些命令仍须在 registry 恢复后使用 `package-lock.json` 对应的真实依赖执行。当前源码已经为正式工具执行清除了本阶段可确认的合同、Hooks、React Refresh、Mock 状态和类型收窄问题。
